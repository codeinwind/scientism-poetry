# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend URL for CORS
FRONTEND_URL=http://localhost:3000

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/scientism-poetry

# JWT Configuration
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRE=30d

# Logging Configuration
# For production (absolute path):
# LOG_DIR=/var/log/scientism-poetry

# For development (relative path):
# LOG_DIR=logs

# Default: If LOG_DIR is not set, logs will be stored in backend/logs directory
